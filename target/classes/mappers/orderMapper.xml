<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.mapper.OrderMapper">

	<select id="getBaskets" parameterType="map" resultType="basket">
		<![CDATA[
			SELECT B.PD_NO, P.PD_IMG, P.PD_NM, P.PD_TAG, P.PD_SALE, B.BSK_QTY
			FROM PRODUCTS_TB P, BASKETS_TB B
			WHERE P.PD_NO = B.PD_NO AND B.MB_ID=#{mbId} AND B.PD_NO IN
		]]>
		<foreach item="item" index="index" collection="productList" open="(" separator="," close=")">
			#{item}
		</foreach>
	</select>
	
	<select id="getProduct" resultType="basket">
		<![CDATA[
			SELECT PD_NO, PD_IMG, PD_NM, PD_TAG, PD_SALE FROM PRODUCTS_TB WHERE PD_NO=#{pdNo}
		]]>		
	</select>
	
	<insert id="insertOrder" parameterType="order">
		<![CDATA[
			INSERT INTO ORDERS_TB VALUES ((SELECT NVL(MAX(ORD_NO), 0)+1 FROM ORDERS_TB), #{mbId}, #{ordNm},
			#{ordZip}, #{ordAddr}, #{ordDeaddr}, #{ordPhone}, #{ordPrice}, #{ordMsg}, 'R', SYSDATE)
		]]>
	</insert>
	
	<select id="getOrderNo" resultType="int">
		<![CDATA[
			SELECT MAX(ORD_NO) FROM ORDERS_TB
		]]>
	</select>
	
	<insert id="insertOrderDetail" parameterType="orderDetail">
		<![CDATA[
			INSERT INTO ORDER_DETAIL_TB VALUES (#{ordNo}, #{pdNo}, #{orddtQty}, #{orddtPrice})
		]]>
	</insert>
	
	<delete id="deleteBaskets" parameterType="map">
		<![CDATA[
			DELETE FROM BASKETS_TB WHERE MB_ID=#{mbId} AND PD_NO IN
		]]>
		<foreach item="item" index="index" collection="productList" open="(" separator="," close=")">
			#{item}
		</foreach>
	</delete>
	
	<update id="updateMileage" parameterType="member">
		<![CDATA[
			UPDATE MEMBERS_TB SET MB_MILE=#{mbMile} WHERE MB_ID=#{mbId}
		]]>
	</update>
	
	<select id="getOrderList" resultType="order">
		<![CDATA[
			SELECT O.ORD_NO, O.ORD_DT, P.PD_NM, O.ORD_PRICE, O.ORD_STATUS
			FROM ORDERS_TB O, ORDER_DETAIL_TB OD, PRODUCTS_TB P
			WHERE O.ORD_NO = OD.ORD_NO AND OD.PD_NO = P.PD_NO AND O.MB_ID=#{mbId}
			ORDER BY O.ORD_NO DESC
		]]>	
	</select>
	
	<select id="getMember" resultType="member">
		<![CDATA[
			SELECT * FROM MEMBERS_TB WHERE MB_ID=#{mbId}
		]]>	
	</select>
	
	<select id="getOrderDetail" resultType="basket">
		<![CDATA[
			SELECT P.PD_NO, P.PD_IMG, P.PD_NM, OD.ORDDT_QTY BSK_QTY, OD.ORDDT_PRICE PD_TAG
			FROM ORDER_DETAIL_TB OD, PRODUCTS_TB P
			WHERE OD.PD_NO = P.PD_NO AND OD.ORD_NO=#{ordNo}
		]]>	
	</select>
	
	<select id="getOrder" resultType="order">
		<![CDATA[
			SELECT * FROM ORDERS_TB WHERE ORD_NO=#{ordNo}
		]]>	
	</select>
	
	<select id="getAllOrderList" resultType="order">
		<![CDATA[
			SELECT * FROM
			(
				SELECT O.ORD_NO, O.ORD_DT, P.PD_NM, O.ORD_PRICE, O.ORD_STATUS, 
				ROW_NUMBER() OVER(ORDER BY O.ORD_NO DESC) AS RNUM
				FROM ORDERS_TB O, ORDER_DETAIL_TB OD, PRODUCTS_TB P
				WHERE O.ORD_NO = OD.ORD_NO AND OD.PD_NO = P.PD_NO
		]]>	
		<include refid="search"></include>	
		<![CDATA[
			)
			WHERE RNUM BETWEEN #{pageStart} AND (#{pageStart} + #{perPageNum} - 1)
		]]>	
	</select>
	
	<sql id="search">
		<if test="searchType != null">
			<if test="searchType == 'n'.toString()">
				AND P.PD_NM LIKE CONCAT('%', CONCAT(#{keyword}, '%'))
			</if>
		</if>
	</sql>
	
	<select id="getStock" resultType="int">
		<![CDATA[
			SELECT PD_STOCK FROM PRODUCTS_TB WHERE PD_NO=#{pdNo}
		]]>	
	</select>
	
	<select id="countAllOrderList" resultType="order">
		<![CDATA[
			SELECT * FROM
			(
				SELECT O.ORD_NO, O.ORD_DT, P.PD_NM, O.ORD_PRICE, O.ORD_STATUS, 
				ROW_NUMBER() OVER(ORDER BY O.ORD_NO DESC) AS RNUM
				FROM ORDERS_TB O, ORDER_DETAIL_TB OD, PRODUCTS_TB P
				WHERE O.ORD_NO = OD.ORD_NO AND OD.PD_NO = P.PD_NO
		]]>	
		<include refid="search"></include>	
		<![CDATA[
			)
		]]>	
	</select>
	
	<update id="modifyCheckedOrder" parameterType="order">
		<![CDATA[
			UPDATE ORDERS_TB SET ORD_STATUS=#{ordStatus} WHERE ORD_NO=#{ordNo}
		]]>	
	</update>
	
	<delete id="deleteOrders" parameterType="list">
		<![CDATA[
			DELETE FROM ORDERS_TB WHERE ORD_NO IN 
		]]>	
		<foreach item="item" index="index" collection="list" open="(" separator="," close=")">
			#{item}
		</foreach>
	</delete>
</mapper>